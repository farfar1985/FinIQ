---
name: Infrastructure decisions and environment details
description: Azure OpenAI Foundry, GitHub farfar1985/FinIQ, Databricks free tier (permission issues), SQLite synthetic data working
type: project
---

Decisions from 2026-03-26 team call + setup session:

1. **LLM access**: All OpenAI and Anthropic connections go through **Azure OpenAI Foundry** (not external URLs).
2. **GitHub**: Personal backup at https://github.com/farfar1985/FinIQ (private). Mars production will use Mars internal GitHub.
3. **QDL (Quandl/Nasdaq Data Link)**: Access added for competitor financial data.
4. **Databricks workspace**: `dbc-af05a0e0-4ebe.cloud.databricks.com`, Free edition, Serverless Starter Warehouse (2XS), catalog = `workspace`.
5. **Databricks permissions BLOCKED**: Free tier denies CREATE TABLE, SELECT, DROP on `workspace.default`. Cesar needs to resolve — either upgrade tier or grant explicit permissions. PySpark script (`databricks_synthetic_data.py`) is ready but can't run.
6. **SQLite synthetic data WORKING**: `finiq_synthetic.db` — 21.4 MB, 165K rows, 17 tables + 3 views. Generated by `generate_synthetic_data_sqlite.py`. No dependencies. Share with team immediately.
7. **Credentials**: All Databricks credentials and API keys stored in Excel file in shared Google Drive folder.

**Why:** Mars requires all connections within their Azure tenant. Synthetic data unblocks development. SQLite is the immediate workaround while Databricks permissions are sorted.

**How to apply:** Use SQLite for development now. Switch to Databricks when Cesar resolves permissions. Architecture docs reference "Azure OpenAI Foundry". Databricks catalog is `workspace` (Mars real catalog is `corporate_finance_analytics_dev`).
