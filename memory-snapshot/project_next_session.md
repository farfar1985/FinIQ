---
name: next-session
description: "2026-06-11 LATE NIGHT — the ENTIRE repo story is built: #767 (PR #768) + #770 (PR #775) + #761 (PR #776) + #773 BUILT+VERIFIED (6 commits on 773-repo-kb-grounding, PR #777 OPENED). #773 verified: route live w/ her session (GitHub auto-approve), script-path real-services for analyze→digest→attach→INDEXED (4 chunks Voyage, both specs), agent mediation TEXTBOOK (cites digest, 2 fitting questions, zero silent staging). Her spec 57a89c84 has the digest attached — her UI grounding test ready. kind-node bytecode corruption diagnosed (3rd machine incident) → LAPTOP NEEDS REBOOT+CHKDSK; fresh OAuth attach fails at clone until then; in-cluster worker scaled 0. PR queue: FIVE open — #766/#768/#775/#776/#777. Then #756 (LOCKED). Full record: CLAUDE.md 2026-06-11 LATE NIGHT entry."
metadata:
  node_type: memory
  type: project
  created: 2026-05-28
  updated: 2026-06-11
  originSessionId: 865f4eec-9f0d-42a4-84f4-46f0ab7b8fca
---

## ⭐⭐⭐ CURRENT STATE (2026-06-11 LATE NIGHT) — read this first

**The ENTIRE repo story is built: import (#767) + orientation (#770) + OOS iteration fix (#761) + repo-in-KB grounding (#773).** PR queue (author farfar1985): **#766** (#669), **#768** (#767), **#775** (#770+#769), **#776** (#761, commit `cfcf5df` — judge prompts recalibrated iterating-vs-locked, "same app?" not "already in spec?"; 3 real-Haiku tests incl. Cesar's verbatim repro). **#773 BUILT + VERIFIED, PR NOT YET OPENED** — branch `773-repo-kb-grounding` (worktree D:/amira-mars-773, 6 commits: P1 digest renderer / P2 create_digest_attachment / P3 RepoDigestWorkflow / P4 attach route + callback / P5a mediation rule + tests / P5b enum-cast fix). **#773 PUSHED + PR OPENED: [PR #777](https://github.com/quantumdatatechnologies/amira-mars/pull/777)** (base master, Closes #773, 6-section body w/ mediation live-proof + sandbox caveat + analyzer-depth note). **Farzaneh UI-tested the full loop herself before the PR**: agent cited the digest, raised dp-feed-fanout + defaulted gaps, she picked pull-on-read, it resolved 2 DPs + 6 warning gaps with audited notes + graph seq 3, kept critical gap-roles open. FIVE PRs now in Cesar queue: #766/#768/#775/#776/#777. Machine rebooted TWICE this night — all repos fsck-clean.

**#773 verification (full record in CLAUDE.md LATE NIGHT entry):** route live with her session (201 → GitHub auto-approve zero clicks → callback fired RepoDigestWorkflow); workflow fail-louded correctly at provision_sandbox (kubeconfig — restored) then clone (host can't reach kind pod IPs — Docker Desktop); **kind-node BYTECODE CORRUPTION diagnosed** ("Executing a cache" at random sites across 3 rebuilds/VM-restart/node-restart; same image clean in plain docker → machine-level, 3rd incident → **LAPTOP NEEDS REBOOT + CHKDSK**; in-cluster worker scaled to 0); every #773-owned stage then verified script-path vs REAL services (real analyze() over real microblog clone → digest → attach → **INDEXED 4 chunks real Voyage on BOTH specs**); **agent mediation TEXTBOOK PASS** on fixture session `96dccbc3` (2× fetch_kb_chunk, cites digest by name + real followers migration, honest about limits, 2 fitting questions [stack unpinned / SSO-vs-local-auth], "Stopping here without staging spec changes", DB confirmed 25 reqs untouched; `spec.out-of-scope-judge-allow` = #761 fix live).

**Her UI test (she asked to do it when back):** her spec `57a89c84` (micro-update app, 10 FR/5 NFR/4 gaps) already has `repo-digest-microblog@a975ef6.md` attached + indexed — she types e.g. "For the follow/follower capability, use the same approach as the attached repo digest — adapt it to our requirements" in the spec chat and should see search → citations → fitting questions, no silent adoption. **Caveat: a FRESH OAuth attach (the full button-path) fails at clone until the kind corruption clears (reboot).** Stack: backend :8000 + worker = D:/amira-mars-773 (P5 live); frontend :3000 = D:/amira-mars spike branch (older UI → mid-stream fragments + stale panels = known artifacts).

- **#770 build** (branch `770-import-interviewer`, commits `3d87134` feat + `48f099f` tests, stacked content-wise on 767 but PR based on master to dodge the squash+delete-branch auto-close): v1.txt orientation entry-rule + route signals `kind="kickoff"` orientation (OOS NOT bypassed — kickoff is wire-mintable; scope-anchored text passes, verified `spec.out-of-scope-kickoff-allow`). 4 real-services tests 3× green + genesis regression 5/5.
- **Farzaneh self-tested in the UI** (fixture `spec-mytest770` / spec `99999999-…`): orientation → her typed SSO+analytics pivot → FR-1 rewritten in place, nested FR-10.x, FR-8 flipped pending, rate-limiting gap resolved ("moves to IdP"), `g-sso-provider` as "Assumption (confirm at build)" → her 3 answers resolved 3 gaps + confirmed FR-1.2/FR-10.2. Composer worked (#769 fix experienced).
- **#773 filed** (repo-as-KB-attachment, mediation mandatory, owner:farzaneh; prereq #669/#766) + **#774 filed** (Build Agent→repo push, no owner, Cesar's tmp-secrets pre-lock: ephemeral k8s secrets per push job, never stored write tokens; browser-IDE prior-art lead).
- **CI**: openapi-snapshot-drift red on #768/#775 is OURS (new endpoints, snapshot not regenerated; #689 precedent = regenerate+push ~10min). Deferred per Farzaneh "forget about the CI". Lint green.
- **Fixtures in dev DB** (cleanup candidates later): spec `66666666-…`/project `55555555-…` (my verify clone) + spec `99999999-…`/project `88888888-…` (her self-test) + the original microblog `1e883f67-…`. All test data, harmless.
- **NEXT (her call, "i dont wanna stop"): #761** (OOS judge blocks new-capability feature-adds during iteration — ours, fix direction in ticket: judge distinguishes iterating-vs-locked, Bloom miss ≠ OOS while iterating; also future-proofs #770's orientation vs the per-turn detector) → **#756** (session cap, LOCKED "build away", 3 phases) → **#773** once #766+#768 merge.

## ⬇️ Earlier 2026-06-11 state (morning/afternoon)

**#767 repo-import is SHIPPED end-to-end.** Rajiv's "reference a GitHub repo → create an initial spec you can modify."
- **What shipped → [PR #768](https://github.com/quantumdatatechnologies/amira-mars/pull/768)** (branch `767-repo-import`, 2 commits `ed3ede8`+`ad994aa` on top of `ae17dcd` WIP): the two-call inference split (`InferenceOutput`→`InferredSpecHalf`+`CapabilityGraphHalf`, in `domain/reveng/types.py`+`inference.py` — fixes the model satisficing on one giant tool-call) + materialize `correlation_id` + `_INFER_TIMEOUT` 5min, AND the **chat handoff**: `POST /specs/{id}/start-refine-session` (`domain/spec/import_routes.py`) + frontend auto-fire (`live-spec-workspace.tsx`, `lib/api/spec.ts`). PR body = locked 6-section shape, `Closes #767`, no owner, no self-merge. **Awaiting Cesar's review/merge.**
- **Live-proven** (Claude-in-Chrome on the running stack): import `miguelgrinberg/microblog` → 14 reqs + 5 gaps; POST the route → session starts + `session_id` stamped + idempotent; signal "expand FR-3" → FR-3.1–3.5 parented to FR-3, original 14 intact (no clobber), nested-rendered in UI after refresh.
- **[#769](https://github.com/quantumdatatechnologies/amira-mars/issues/769) FILED** (`bug`+`needs-design`+`track:ai-agent`+`track:frontend`, no owner) — **imported-spec composer stuck "Responding…" until first turn completes** (session attaches idle → frontend never sees turn-complete → queues input). Greeting-turn fix prototyped + **REVERTED** (trips the empty-graph OOS judge #624). Two fixes in ticket: (A) greeting w/ OOS-awareness, (B) frontend idle-state (#611). **#769's fix = the import→interviewer feature's seam (Q3) — same work.** To fix today/tomorrow.
- **ACTIVE: Spec-Agent maturity design discussion** (Farzaneh's Q1-Q4, she said "a must"). Full directions in **`project_spec_agent_repo_import_roadmap.md`**. Headline: empowered interviewer's depth is independent of seed → **repo-import + interviewer-on-top = FinIQ-grade spec**. Recommended next build = **Option A: import → interviewer auto-deepens** (repo-aware opening, adapts "spec-as-is" vs "build-on-top"; doubles as the #769 fix). Q1 answered; Q2/Q3/Q4 captured; she may clarify more.
- **TOPOLOGY (bank it):** local stack is SPLIT — **:8000 backend = `D:/amira-mars-test`** (`767-repo-import` branch, `uvicorn --reload`) ; **:3000 frontend = `D:/amira-mars`** on `spec-agent-completeness-spike` (different branch — frontend auto-fire not exercised there; POST the route manually = same effect). `-test` has no node_modules. Worker restarted from `-test`. Before any live drive: pin which worktree/branch serves each port (`Get-NetTCPConnection :3000/:8000` + `pgrep -af amira_api` + `/proc/PID/cwd`).
- **GitHub safety (PRIVATE, never in PRs/tickets/Cesar comms):** repo-import testing never touches the QDT repo — public/sample repos only. See `feedback_github_safety_import_testing.md`.
- **Prior lockdown queue still pending behind this** (from 2026-06-10, unchanged): #756 LOCKED-but-unbuilt (session cap, 3 phases), #761 (OOS blocks feature-adds during iteration), #751, #690, #669 (KB grounding — needs Cesar's 6 lock answers + VOYAGE_API_KEY), #703/704/705; closes #724 (verdict at `Temp/issue_724_comment.md`)/#694/#722; then Build Agent loop #731/#732. Nudge #725 (Spec→Build handoff).

---

## ⬇️ HISTORY BELOW (2026-06-10 and earlier)

## ⭐ STATE (2026-06-10 FINAL): **ALL 4 PRs MERGED by Cesar (~21:38-21:42 UTC: #753, #757, #758, #759 — auto-closing #750/#754/#701/#702/#755). Master = `a378675`. 9 tickets closed today incl. #733 merged+deployed.** **#756 LOCKED by Cesar ("build away") — all 6 picks = our recommendations verbatim.** **#724 STRESS-VERIFIED FIXED on post-merge master** (headless drive, master worktree `D:/amira-mars-test`: killer "resolve-everything" turn → 4 iterations, bounded slice, honest done/not-done/next reply [#728 cap + #733 disposition + #758 fidelity in concert]; 18-KPI list bomb → parent FR-2-kpi-catalog + batch 1 of 3 (6 children) + pre-announced batches 2-3; ZERO ActivityError; verdict comment drafted at `C:/Users/farza/AppData/Local/Temp/issue_724_comment.md`, post+close pending). **#761 NEW (Cesar, from his live v1.0.60 verify): OOS judge blocks new-capability feature-adds during ITERATION** (classifier binding-or-schema 0.75 → second-pass block; same instruction rephrased to existing-FRs-only passes) — fix direction in ticket: judge must distinguish iterating vs locked spec; Bloom "not in graph" must NOT imply OOS while iterating. **#669 side-bug PROVEN live**: `KbAttachmentStatus.INDEX_FAILED` AttributeError (enum = INDEXING/INDEXED/ERROR). **TOMORROW = SPEC AGENT LOCKDOWN** (deal with Farzaneh: agent-surface beats ownership labels — close EVERYTHING Spec-related): #761 → #756 → #751 → #690 → #669 → #703/704/705 + closes #724/#694/#722 + final Spec-Agent sweep + nudge #725/#669-lock/Voyage-key. **THEN BUILD AGENT loop: #731 + #732** (drive, file, fix — same playbook). Cesar parallel: Helm/CI-CD #633 (~4d Mars production deployment). **#756 BRAINSTORM DONE + POSTED on the ticket** (issuecomment-4674158210; proposal doc `D:/Amira FinIQ/756_session_cap_design.md`) — **AWAITING CESAR'S LOCK on 6 questions** (D1 leak fix = workflow-exit emission rec / D4 axis = dual org-ceiling-50 + per-user-10 rec / cleanup scope = org_admin role flag / D2 location / zombie backfill / companion cap-bypass). **#669 BRAINSTORM ALSO DONE + POSTED** (issuecomment-4674565638; doc `D:/Amira FinIQ/669_kb_grounding_design.md`) — headline: PR #455's body explicitly designated #669 as the retrieval follow-up; `kb_chunk` is write-only (zero reads in codebase); rec = `fetch_kb_chunk(query,k)` semantic search via new `embed_query` sibling (EMBED-1) + `SpecContext.kb_attachments` context listing (#757 pattern) + conditional-injected evaluator criterion (#758 pattern); ~1.5-2d, ZERO migrations; prereq = dev VOYAGE_API_KEY; 2 verified side-bugs found (kb_indexer.py:390 `KbAttachmentStatus.INDEX_FAILED` doesn't exist → failure-handler AttributeError, attachment stuck "indexing" forever; picker accepts .xlsx/.json that backend 400s). Build order: #756 first (committed), #669 after his nod. **Key study findings:** cancel route = SOLE session-ended emitter (plan/05 T-M2-05 slot is the designed fix home); `actor_user_id` is a real COLUMN on outbox_event → per-user cap needs ZERO migrations; Workspace is a real table but sessions don't record it → per-workspace deferred; **NEW: companion turns BYPASS the cap entirely** (`companion/api/routes.py:360` starts the workflow directly, not via start_agent_session — verified myself after a subagent mis-read it as an O(N) leak; it's an enforcement GAP, not a leak); count-query JSONB correlation is unindexed. When Cesar locks → build (Phase 1 leak ~2d, Phase 2 axis ~0.5d, Phase 3 cleanup UI ~1.5d). Then pitch #669. #733 was MERGED + DEPLOYED by Cesar today (his tweak `214c52c`: warning/info gaps must self-label `Assumption (confirm at build): <default>`). spec.writer account test = full pass (sign-in/spec/lock/e-sign/Build-correctly-DENIED — AUTHZ gating works; baton-pass question flagged). **QUEUE STATE: #755 DONE (PR #759) → #756 brainstorm DONE+POSTED (awaiting Cesar's lock → then BUILD) → pitch #669 (KB grounding) after.** #722/#694/#690 verified-resolved; CESAR closes them (his call). #699 already done (PR #712, 05-29). **KEY FINDING BANKED in PR #758**: evaluator healthy-controls flake ~17% at master baseline (never deterministic); every static-rubric paragraph makes the judge stricter on unrelated dims → conditional rubric injection (`_looks_multi_action` in evaluator.py) is the calibration-neutral architecture. Worktrees committed+pushed+clean: D:/amira-mars-750, -754, -701; -729's branch merged (worktree removable).

**#729 build (PR #733):** worktree `D:/amira-mars-729`, branch `729-gap-classification` off master `7a2a03a`, commit `a16399b`. 3 files: `v1.txt` (raise_gap WHAT-vs-HOW disposition rule — `critical`=requirement-decision-no-default=block+ask=spec-kit `[NEEDS CLARIFICATION]`; `warning`/`info`=build-config-with-default="confirm-at-build", don't block; + terminal criterion "no open `critical` gaps" + Integrations config→warning/info), `evaluator.txt` (symmetric gap-disposition penalty: config-as-critical = over-blocking AND no-default-requirement-as-warning = under-flagging), + new real-Opus `test_elicit_turn_gap_disposition.py`. **Verified:** gap-disposition 3× green (47.6/73.2/52.6s) + interview/evaluator regression 10 passed + ruff clean. PR #733 = locked 6-section body, `Closes #729`, no owner label, no self-merge. Design proposal: `D:/Amira FinIQ/729_gap_classification_design.md`.

The morning's empowered-interviewer validation + a **medium-prompt test** (expense app, kickoff + 7 turns → 38 FR comprehensive spec; finding → **#729**) + Rajiv's **build-agent test** (tip calculator: Spec → e-sign → Build → live preview, AC-faithful; findings → **#731** + the governance-link 404 Cesar fixed live) are all done. The active build assignment is now **#729** (Cesar approved, design-first). Full detail: CLAUDE.md `2026-06-09` entries.

PR #728 is done. Cesar **merged it** (squash `ce0f9f0`), **improved the OOS fix** (`23784d0`), and **redeployed** — the empowered Spec Agent interviewer is **live on `amira.qdt.ai`**. We validated a simple spec end-to-end on the cluster this morning. The immediate next step is a **medium-complexity test**, then a Rajiv demo.

## What Cesar changed on merge (`23784d0` — his improvement on our cap-fix)
- Our PR **raised** the OOS judge caps (`rationale` 500→2000, `suggested_remediation` 300→600). Cesar **REMOVED them entirely** — `OutOfScopeJudgeOutput.rationale: str = Field(min_length=1)`, `suggested_remediation: str = Field(default="")`. His reasoning: raising "only moved the wall (800 tokens ≈ 3200 chars can still exceed); removing it kills the class — same as the #681 regex removal."
- **He also found a SECOND latent cap I'd missed:** `agents/spec/out_of_scope.py` `OutOfScopeVerdict.reason` was capped at 160 chars → removed → `reason: str = Field(min_length=1)`. That's the **per-turn detector** (runs on every refinement), not the judge — its own live false-block source.
- **Lesson (banked in `feedback_smoke_test_llm_tool_use_pre_commit.md`):** REMOVE a free-text-cap-vs-token-budget cap, don't raise it; and when the false-block is in an OOS path, trace BOTH schemas (`oos_judge.py` judge + `out_of_scope.py` detector). I'd only audited one.

## What was validated LIVE on the cluster (2026-06-09)
- **Simple to-do spec** — `amira.qdt.ai/spec/db2cf350-c3ff-40b8-9213-4902825ff691`. Farzaneh drove the UI; I read via screenshots (no cluster DB). Agent interviewed aspect-by-aspect, reached terminal state: *"v1 spec is COMPLETE and ready to lock. … 14 FRs, 4 NFRs, 13 ACs, 11 nodes wired as a DAG (10 edges), data model in FR-11, PWA deployment in FR-9/10, no open gaps, no open decision points. To lock it in, use the **Route for E-Signature** control at the top of the workspace."*
- **Terminal-message UX fix works live** (declares complete + points to the button, does NOT invite a chat-"lock it" — the exact confusion from the 2026-06-08 local tip-calc test where "yes, lock it" got OOS-blocked at 0.30<0.50). **0 false-blocks** on the simple run; proportionate (no enterprise cruft).

## ⭐ NEXT — test a MEDIUM-sized prompt on `amira.qdt.ai`
- Pick a mid-complexity app (between the simple to-do and the deep financial platform) — e.g. a small SaaS with auth + a couple of integrations + a data model. Farzaneh types the kickoff in the UI; I read each turn via screenshots and tell her what to answer to the interview questions (same flow as the validated runs).
- **Watch for:** the over-bloom edge (finding #1 — `elicit_turn` 12-iteration cap on big turns; recovery = refresh + scoped re-send) once the spec grows; confirm the interview-to-complete flow holds + reaches the terminal "ready to lock"; confirm the OOS hardening holds (0 false-blocks) on a deeper-than-simple spec.
- **No DB access on the cluster** → the rendered spec + screenshots are the primary signal; `temporal.amira.qdt.ai` (shared-team creds in `project_temporal_ui_access.md` — NEVER echo the password) is the backup debug channel if a turn looks stuck.
- After medium passes → **demo/test with Rajiv.**

## 4 findings still Cesar's lane (flagged in #728, NOT ours to fix)
1. `elicit_turn` 12-iteration ReAct cap strained on large specs → over-bloom `ActivityError` (recovery = refresh + scoped re-send).
2. OOS judge over-blocks control/meta commands ("yes, lock it" → 0.30 < 0.50 floor); lock is the Route-for-E-Signature button, not chat.
3. After an `ActivityError` the composer queues against a finished turn + Retry is a no-op (#723) → only refresh recovers.
4. No visible "ready to lock" status/CTA in the UI (terminal-message helps from the agent side; a status flip is the UI side).

## Open follow-ups beyond the interviewer (Cesar's lane / pending decision)
- **#729** (filed + **APPROVED by Cesar 2026-06-09 → design-first, OURS to build — THE ACTIVE ASSIGNMENT**) — **gap classification for convergence.** Finding: on a rich spec the agent doesn't self-terminate at GAPS=0 and files build/deploy-config specifics (NetSuite auth, SCIM push/poll, Graph mailbox, tenant config) as **blocking** gaps. Cesar's verdict: *"the issue is right: block-and-ask vs default-and-flag"* — but it touches **5 surfaces** (gap tool / readiness gate / prompt / UI / Build handoff) → brainstorm-first. **His 4 constraints (the contract):** (1) reuse the existing `raise_gap` `severity` field (only `critical` blocks lock) OR add an explicit `disposition` field — decide in brainstorm; (2) **mirror the existing spec-kit skill** (`[NEEDS CLARIFICATION]`=critical-no-default→block; default→defer-and-flag; spec-vs-plan separation; ready when no critical clarifications remain) — NOT a parallel scheme; (3) every default-and-confirm-at-build assumption VISIBLE to the Approver at e-sign + passed to the Build Agent, never silently buried; (4) agent PROPOSES ready, human pulls the lock (never auto-lock); skip the MVP/standard/exhaustive depth dial. Longer-term (#726): lean on the spec-kit + superpowers skills, not a home-grown prompt copy. **PLAN: Phase 0 study → Phase 1 brainstorm D1-D4 → Phase 2 surface to Cesar + lock → Phase 3 plan → Phase 4 build TDD → Phase 5 PR.** Cross-links #726 + #725.
  - **STATUS 2026-06-09: Phase 0 + brainstorm DONE (read all 4 surfaces + the spec-kit reference).** **Located the spec-kit skill** (Farzaneh said find it, don't ask Cesar): the reference is `D:/refs/spec-kit` (cloned during the 2026-05-21 assessment); the "skill we added" = `apps/api/src/amira_api/chat/slash_commands.py` (T-M3-102, the `/specify`→`/clarify`→`/plan`→`/analyze`→`/tasks`→`/implement` flow mirroring spec-kit). spec-kit's model = WHAT/WHY-vs-HOW + `[NEEDS CLARIFICATION]` markers (block+ask, only when no reasonable default) + spec-vs-plan phase separation (config → /plan) + completeness = "no markers remain". **Brainstorm converged → recommend D1 = Option A** (reuse `severity`; fix in `v1.txt` + `evaluator.txt` ONLY; mirror spec-kit's WHAT/HOW rule; NO typed `disposition` field — that's the "parallel scheme" Cesar warned against). **Headline finding:** the lock gate (`readiness.py::_evaluate_open_gaps` blocks only on `critical`) + serializer (`serialize.py` renders unresolved gaps of any severity → build) + approver view (`SpecVersionView.gaps`) ALREADY handle non-blocking gaps → Option A collapses Cesar's expected 5 surfaces to **2 files + 1 test, zero schema/migration**. One deliberate deviation flagged: pure spec-kit keeps config out of the spec; we keep a lightweight non-blocking record IN the spec for Cesar's #3 visibility. **Design proposal: `D:/Amira FinIQ/729_gap_classification_design.md`.** Richness preserved (disposition≠depth; the interviewer still blooms the full FR/NFR/AC). **AWAITING Cesar's D1 lock (Option A vs B) + §6-deviation OK → THEN Phase 3 plan + Phase 4 build. — SUPERSEDED 2026-06-09: Cesar LOCKED D1=Option A; #729 is BUILT + PR #733 OPEN (commit a16399b, worktree D:/amira-mars-729; 3x green + 10-passed regression + ruff clean), awaiting his review+merge. See STATE header above for the build detail.**
- **#731** (filed 2026-06-09, `bug` / `owner:cesar` / `track:ai-agent` / `track:frontend`) — **Build Agent: mid-build ActivityError + Retry no-op.** From the build test: build hit an ActivityError ~88s in (before writing `app/page.tsx`; preview 502), in-UI **Retry was a no-op**, a chat "continue" message recovered it → completed + live. **Key insight:** the Retry no-op exists in BOTH the spec chat (**#723**) and the build chat → almost certainly ONE shared AmiraChat `regenerate()` bug → single fix covers both. Cesar asked us to file it; he'll fix after the Mars demo-account setup. ActivityError root cause not captured (CONSOLE shows only `reason: "ActivityError"`; needs Temporal — Cesar has cluster access; recovered on retry → likely transient; may share a root with the 502).
- **#732** (filed 2026-06-09, `bug` / `owner:cesar` / `track:ai-agent` / `track:backend`) — **Build-time Compliance Matrix stays empty (0/0 → 0%) after a successful build.** Code-traced on master `7a2a03a` (NOT #725 — confirmed by reading the code): matrix populated by a SEPARATE pipeline (Build Agent `_emit_compliance_trigger` → standalone `compliance.recompute_consumer` process → `detector.recompute()` loads the **capability graph**, not the #725-stripped spec.md → upserts rows → `compute_readiness_score` = 0 on empty). Empty ⇒ most likely the **`recompute_consumer` process isn't running on the cluster** (same class as the audit-log consumer not running in dev). Secondary: `_dispatch_recompute` uses an `_EmptyFileTree` (real sandbox FileOpsReader not wired, "Phase C.3") → scores vs spec text not built code. 2 cluster checks for Cesar in the body. **Lesson: code-trace a cross-bug causal link before raising it (we almost mis-attributed this to #725).**
- **#725** — Spec→Build handoff: `render_spec_markdown` (`serialize.py`) drops the capability graph (architecture/edges) + reads ACs from `spec_requirement` while the agent stores them in the graph → the build gets 0 ACs + no architecture. Filed as a FINDING/question; recommended fix in the body. NOT ours to fix.
- **#726** — Empowered Spec Agent capability roadmap (web grounding / UI-UX aspect / system-assigned IDs); #728 was the first increment, doesn't close it.
- **#681 → PR #689** — symmetric requirement-CRUD + nested sub-FRs; OPEN, awaiting Cesar's merge. Related: the agent has no tool to deprecate capability nodes/edges/ACs (backend #693 ready; only the tool surface is missing) — commented on #681.
- **#206 / T-M5-16** — Companion MCP handler, the LAST open milestoned ticket (owner:cesar, was ours). Pre-build recon in `project_companion_mcp_206_recon.md`: ~80% clear; one gate = permission-intersection contract needs ~15-30min brainstorm + Cesar sign-off. If picked up, build vs FRESH master (spike branch's companion workflow.py regressed).

## Things NOT to do
1. **No git push / no merge without Farzaneh's per-action go.** Cesar merges; the spike tree's edits stay LOCAL/uncommitted (revert later — production = his merged #728).
2. **Per-action confirm every GitHub write.**
3. Don't fix Cesar's seams (the 4 findings, #725) ourselves — flagged for him.
4. Never print the Temporal UI password or any `.env` value.

## Quick-resume facts
- The empowered interviewer is on **current master + deployed** — no local stack needed to test (use `amira.qdt.ai` directly).
- Drive the deployed UI via Farzaneh + screenshots (browser chat reads privacy-block on session-UUID; cluster has no DB/log access for us). The URL `/spec/<uuid>` is the **spec_version_id**.
- gh authed (`farfar1985`); `gh` needs `/c/Users/farza/bin` on PATH in Git Bash.
- Worktree `D:/amira-mars-interviewer` (branch `spec-agent-empowered-interviewer`) can be torn down — #728 merged. Spike tree `D:/amira-mars` still on `spec-agent-completeness-spike` with uncommitted edits (revert later).

## Source of truth
- **CLAUDE.md `2026-06-09` entry** — this morning's merge+deploy+cluster-validation (READ FIRST), and `2026-06-08` for the PR ship.
- **`project_empowered_spec_agent_spike.md`** — the spike + PR #728 + merge/deploy record.
- **`feedback_smoke_test_llm_tool_use_pre_commit.md`** — the cap-vs-token-budget lesson (now: REMOVE the cap, trace BOTH OOS schemas).
- PR **#728** body — comprehensive writeup + per-piece validation table + the 4 findings.
